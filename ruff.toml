line-length = 100
target-version = "py313"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[lint.per-file-ignores]
"src/dashboard/routes/*.py" = ["B008"]  # Depends() in defaults is idiomatic FastAPI

[format]
quote-style = "double"
